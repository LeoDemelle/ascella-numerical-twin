# Numerical twin of Ascella by Qandela

## Installation
This implementation uses the quantum computing framework StrawberryFields by Xanadu. To install all dependencies :
```bash
python -m venv .venv
.venv/Scripts/activate
pip install -r requirements.txt
```

